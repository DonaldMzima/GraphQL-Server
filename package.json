{
  "name": "graphql-server-example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "compile": "tsc",
    "start": "yarn compile && node ./dist/index.js",
    "start:dev": "nodemon --watch 'src/**/*.ts' --exec 'yarn start'"
  },
  "dependencies": {
    "@apollo/server": "^4.9.3",
    "graphql": "^16.8.0"
  },
  "devDependencies": {
    "@types/node": "^20.6.0",
    "typescript": "^5.2.2",
    "nodemon": "^2.0.15"
  }
}
